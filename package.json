{
	"name": "labelr",
	"version": "1.0.0",
	"description": "A really awesome way to manager labels for github issues.",
	"main": "index.js",
	"scripts": {
		"start": "hjs-dev-server",
		"prebuild": "rm -rf public && mkdir public",
		"build": "NODE_ENV=production webpack",
		"deploy": "npm run build && surge -p public -d labelr-ishan1608.surge.sh",
		"yolo": "npm run build && npm run deploy"
	},
	"author": "Ishan <ishan1608@gmail.com>",
	"license": "MIT",
	"dependencies": {},
	"devDependencies": {
		"@babel/core": "^7.2.2",
		"@babel/preset-env": "^7.2.3",
		"babel-loader": "^8.0.4",
		"hjs-webpack": "^9.2.0",
		"surge": "^0.20.1"
	}
}
